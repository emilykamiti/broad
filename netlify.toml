[build]
command = "npm run build"
publish = "build"

# For Vite's client-side routing
[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# Environment settings
[build.environment]
NODE_VERSION = "18"